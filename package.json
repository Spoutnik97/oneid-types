{
  "name": "oneid-types",
  "version": "1.1.4",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "deploy": "yarn build && yarn version && git push"
  },
  "dependencies": {
    "typescript": "^4.1.2"
  }
}
