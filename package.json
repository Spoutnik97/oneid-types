{
  "name": "oneid-types",
  "version": "1.5.3",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "deploy": "yarn build && git commit -a -m 'built files' && yarn version && git push --follow-tags"
  },
  "dependencies": {
    "typescript": "^4.1.2"
  }
}
